<style type="text/css">
  .Zipcode_filter{
    float: right;
    display: inline-flex;
  }
  .form-control{
    display: inline;
  }
  .search_area #keywords{
    width: 75%;
  }
  .go-btn{
    float: right;
  }
</style>
<div class="search_area">
  <a href="javascript:void(0);" class="close_this search_toggle"></a>
  <% @taxons = @taxon && @taxon.parent ? @taxon.parent.children : Spree::Taxon.roots %>
  <%= form_tag spree.products_path, method: :get do %>
    <!-- <div class="form-group"> -->
      <%# cache(cache_key_for_taxons) do %>
        <%#= select_tag :taxon,
              options_for_select([[Spree.t(:all_departments), '']] +
                                    @taxons.map {|t| [t.name, t.id]},
                                    @taxon ? @taxon.id : params[:taxon]), 'aria-label' => 'Taxon', class: "form-control" %>
      <%# end %>
    <!-- </div> -->
    <%= search_field_tag :keywords, params[:keywords], placeholder: Spree.t(:search) %>
    <%= button_tag '', name: nil, class: "fa fa-search search-btn" %>
  <% end %>
<!--    <div class="Zipcode_filter">
  <% if spree_current_user %>
  <input type="text" name="zipcode" id="zipcode" class="form-control" value="<%= spree_current_user.zip_code %>" placeholder="Enter Zipcode" /><br>
  <% else %>
  <input type="text" name="zipcode" id="zipcode" value="" placeholder="Enter Zipcode" /><br>
  <% end %>
  <%= link_to 'Go', '#',:class => 'go-btn' %>
</div> -->
</div>
